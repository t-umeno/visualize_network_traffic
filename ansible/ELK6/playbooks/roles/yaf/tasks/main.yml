- name: install apt packages
  apt:
    name: "{{ packages }}"
    update_cache: yes
  vars:
    packages:
    - libglib2.0-dev
    - libpcap-dev
    - libndpi-dev
    - libndpi-bin
  become: yes
- name: compile yaf
  script: scripts/compile_yaf.sh
  become: yes
