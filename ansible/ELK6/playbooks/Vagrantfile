# -*- mode: ruby -*-
# vi: set ft=ruby :

$visualize_network_traffic_config_type = "elasticsearch_1node"
load "./Vagrantfile.local" if File.exist?("./Vagrantfile.local")

load "./Vagrantfile.elasticsearch_1node" if $visualize_network_traffic_config_type == "elasticsearch_1node"
load "./Vagrantfile.elasticsearch_file_1node" if $visualize_network_traffic_config_type == "elasticsearch_file_1node"
load "./Vagrantfile.elasticsearch_3nodes" if $visualize_network_traffic_config_type == "elasticsearch_3nodes"
load "./Vagrantfile.aws_ec2_1node" if $visualize_network_traffic_config_type == "aws_ec2_1node"
load "./Vagrantfile.elasticsearch_snort_1node" if $visualize_network_traffic_config_type == "elasticsearch_snort_1node"
load "./Vagrantfile.opendistro_1node" if $visualize_network_traffic_config_type == "opendistro_1node"
load "./Vagrantfile.opendistro_apt_1node" if $visualize_network_traffic_config_type == "opendistro_apt_1node"
load "./Vagrantfile.elastiflow_docker_grafana_1node" if $visualize_network_traffic_config_type == "elastiflow_docker_grafana_1node"
